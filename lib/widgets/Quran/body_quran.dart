import 'package:flutter/material.dart';

class BodyQuran extends StatelessWidget {
  const BodyQuran({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
