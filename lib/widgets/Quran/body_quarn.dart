import 'package:flutter/material.dart';

class BodyQuarn extends StatelessWidget {
  const BodyQuarn({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column();
  }
}
